#! @shell@ -e

userDir=$1
dir=$(@coreutils@/bin/dirname $userDir)
base=$(@coreutils@/bin/basename $userDir)
lxcDir=$dir/$base

name=$2
shift 2

[ -f $lxcDir/lxc.conf ]

@lxc-start@ -n $name -f $lxcDir/lxc.conf -L $lxcDir/console -o $lxcDir/console -p $lxcDir/pid @daemon@ -- $@
