#! @shell@ -e

base=/var/lib/lxc
dir=$base/@name@
gcbase=@gcbase@
gcdir=$gcbase/tsp/@name@

[ -d $dir ]

@coreutils@/bin/rm -f $dir/config $dir/fstab $dir/creator $dir/creator $dir/sterilise
@coreutils@/bin/rm -rf $dir/rootfs/nix $gcdir

for f in @onSterilise@; do
    $f $dir
done
